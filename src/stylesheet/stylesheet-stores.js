import { StyleSheet } from 'aphrodite'

export const styles = StyleSheet.create({
	
	storeBackground: {
		backgroundColor: 'skyblue',
		textAlign: 'left',
		color: 'black',
	},

	discountsLink: {
		textAlign: 'right'

	},
});
